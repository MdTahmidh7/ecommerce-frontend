server {
    listen 80;
    server_name your_domain.com www.your_domain.com;

    root /var/www/html/ecommerce-frontend/browser;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}